name: reusable-concurrency
on: workflow_call
concurrency:
  group: act-test-reusable-group
defaults:
  run:
    shell: powershell
jobs:
  called:
    runs-on: self-hosted
    steps:
      - run: echo called
