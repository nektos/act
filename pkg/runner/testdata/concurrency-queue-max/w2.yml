name: qw2
on: push
concurrency:
  group: act-test-queue-max
  queue: max
defaults:
  run:
    shell: bash
jobs:
  a:
    runs-on: self-hosted
    steps:
      - run: |
          touch "$MARKER_DIR/qw2-start"
          sleep 2
          touch "$MARKER_DIR/qw2-end"
