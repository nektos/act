name: sw1
on: push
concurrency:
  group: act-test-supersede
defaults:
  run:
    shell: bash
jobs:
  a:
    runs-on: self-hosted
    steps:
      - run: |
          touch "$MARKER_DIR/sw1-start"
          sleep 2
          touch "$MARKER_DIR/sw1-end"
