name: sw2
on: push
concurrency:
  group: act-test-supersede
defaults:
  run:
    shell: bash
jobs:
  a:
    runs-on: self-hosted
    steps:
      - run: |
          touch "$MARKER_DIR/sw2-start"
          sleep 2
          touch "$MARKER_DIR/sw2-end"
