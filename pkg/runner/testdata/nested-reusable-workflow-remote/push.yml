name: Test Nested Reusable Workflows
on: push

jobs:
  call-remote-workflow:
    uses: nektos/act-test-actions/.github/workflows/main.yml@main
    with:
      message: "Hello from caller"
    secrets: inherit
