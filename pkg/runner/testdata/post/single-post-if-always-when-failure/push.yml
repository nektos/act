name: single-post-if-always-when-failure
on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./actions/post/node12-post-if-always
      - run: exit 1
